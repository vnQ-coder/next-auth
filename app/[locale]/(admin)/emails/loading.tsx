import { Card } from "@/components/ui/card";

export default function loading() {
  return (
    <Card className="w-full h-[calc(100vh-120px)] p-4 flex items-center justify-center">
      Loading...
    </Card>
  );
}
